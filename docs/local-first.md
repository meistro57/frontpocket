# Local-First Operation

Default embedding provider is Ollama.

```env
EMBEDDING_PROVIDER=ollama
```

FrontPocket can run fully on local infrastructure with Docker Compose.
Cloud providers remain optional and explicitly configured.
